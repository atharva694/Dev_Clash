import { Bell, Search } from "lucide-react";
import { motion } from "framer-motion";

export function TopBar() {
  return (
    <header className="h-24 w-full flex items-center justify-end px-10 bg-transparent z-40 relative">
      {/* Empty space reserved for future TopBar elements */}
    </header>
  );
}
